<% if @feed_items.any? %>
  <ol class="microposts">
    <% @feed_items.each do |feed| %>
      <%= render feed %>
    <% end %>
  </ol>
  <%= will_paginate @feed_items, params: { controller: :static_pages, action: :home } %>
<% end %>